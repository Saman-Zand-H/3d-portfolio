export type Skill = {
  name: string;
  imageUrl: string;
};

export type Experience = {
  company_name: string;
  title: string;
  date: string;
  icon: string;
  iconBg: string;
  points: string[];
};
